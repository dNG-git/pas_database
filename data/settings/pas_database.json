# direct PAS
# Python Application Services
#
# This is the initial extended JSON configuration file. Comments are removed
# if edited. Comments must be removed before reading this JSON file with an
# unsupported JSON parser.

{
 # Deactivate threading for serialized access to the underlying database.
 # This is required for SQLite databases.
 # "pas_database_threaded": false,

 # Database URL given to SQLAlchemy.
 # http://docs.sqlalchemy.org/en/latest/core/engines.html#sqlalchemy.create_engine
 # "pas_database_url": "sqlite:///data/db.sqlite3"

 # Database options given to SQLAlchemy.
 # "pas_database_sqlalchemy_echo": "debug"
}